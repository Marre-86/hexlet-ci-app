name: artem
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "The job was automatically triggered by a ${{ github.event_name }} event."
      - run: echo "Запущено на ОС: $RUNNER_OS"
      - name: Check out repository code
        uses: actions/checkout@v3
      - run: make setup
      - run: make test
      - run: make lint
